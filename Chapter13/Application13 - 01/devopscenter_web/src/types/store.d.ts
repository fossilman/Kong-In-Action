interface RootState {
    kongStatus: string;
    isCollapse: boolean;
    screen: number;
}

interface State {
    kongStatus: string;
    isCollapse: boolean;
    screen: number;
}

interface UserInfo {
    name?: string;
}
